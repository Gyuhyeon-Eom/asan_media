# DB 접속 설정 - 로컬에 db_config.local.yaml로 복사해서 사용
# 회사 PC (Windows, Docker Desktop)
database:
  host: localhost
  port: 5432
  dbname: asan_media
  user: sundo
  password: "sundo123!@#"
  schema: asan_media
